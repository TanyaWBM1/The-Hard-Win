# Secrets — NEVER commit these (live API keys / tokens)
credentials.env
supabase.env
*.env

# Python
__pycache__/
*.pyc

# Logs
post-log.txt
comment-intake-log.txt

# Dependencies (regenerate with: npm install)
node_modules/

# Rendered cards — regenerable from records + render_card.py, and hosted in Supabase
output/

# OS junk
.DS_Store
Thumbs.db
